services:
  meraki-mcp-servers:
    build:
      context: ./meraki-mcp-server
      dockerfile: Dockerfile
    container_name: meraki-mcp-server
    restart: unless-stopped
    
    # Load environment variables from .env file
    env_file:
      - ./meraki-mcp-server/.env
    
    # Environment variables (override any from .env file if needed)
    environment:
      - MCP_HOST=0.0.0.0
      
    # Port mapping: host:container
    ports:
      - "8000:8000"
    
    # Volume mounts (optional)
    volumes:
      # Mount configuration files (read-only)
      - ./meraki-mcp-server/openapi:/app/openapi:ro
    
    # Security options
    security_opt:
      - no-new-privileges:true
    
    # Resource limits (optional, adjust as needed)
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: '0.5'
        reservations:
          memory: 256M
          cpus: '0.25'
    
    # Logging configuration
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  netbox-mcp-server:
    build:
      context: ./netbox-mcp-server
      dockerfile: Dockerfile
    container_name: netbox-mcp-server
    restart: unless-stopped
    
    # Load environment variables from .env file
    env_file:
      - ./netbox-mcp-server/.env
    
    # Environment variables (override any from .env file if needed)
    environment:
      - MCP_HOST=0.0.0.0
      
    # Port mapping: host:container
    ports:
      - "8001:8001"
    
    # Security options
    security_opt:
      - no-new-privileges:true
    
    # Resource limits (optional, adjust as needed)
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: '0.5'
        reservations:
          memory: 256M
          cpus: '0.25'
    
    # Logging configuration
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  catc-mcp-server:
    build:
      context: ./catc-mcp-server
      dockerfile: Dockerfile
    container_name: catc-mcp-server
    restart: unless-stopped
    
    # Load environment variables from .env file
    env_file:
      - ./catc-mcp-server/.env
    
    # Environment variables (override any from .env file if needed)
    environment:
      - MCP_HOST=0.0.0.0
      
    # Port mapping: host:container
    ports:
      - "8002:8002"
    
    # Security options
    security_opt:
      - no-new-privileges:true
    
    # Resource limits (optional, adjust as needed)
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: '0.5'
        reservations:
          memory: 256M
          cpus: '0.25'
    
    # Logging configuration
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  ios-xe-mcp-server:
    build:
      context: ./ios-xe-mcp-server
      dockerfile: Dockerfile
    container_name: ios-xe-mcp-server
    restart: unless-stopped
    
    # Load environment variables from .env file (optional)
    env_file:
      - ./ios-xe-mcp-server/.env
    
    # Environment variables (override any from .env file if needed)
    environment:
      - MCP_HOST=0.0.0.0
      - MCP_PORT=8003
      
    # Port mapping: host:container
    ports:
      - "8003:8003"
    
    # Security options
    security_opt:
      - no-new-privileges:true
    
    # Resource limits (optional, adjust as needed)
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: '0.5'
        reservations:
          memory: 256M
          cpus: '0.25'
    
    # Logging configuration
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  thousandeyes-mcp-server:
    build:
      context: ./thousandeyes-mcp-server
      dockerfile: Dockerfile
    container_name: thousandeyes-mcp-server
    restart: unless-stopped
    
    # Load environment variables from .env file
    env_file:
      - ./thousandeyes-mcp-server/.env
    
    # Environment variables (override any from .env file if needed)
    environment:
      - MCP_HOST=0.0.0.0
      
    # Port mapping: host:container
    ports:
      - "8004:8004"
    
    # Security options
    security_opt:
      - no-new-privileges:true
    
    # Resource limits (optional, adjust as needed)
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: '0.5'
        reservations:
          memory: 256M
          cpus: '0.25'
    
    # Logging configuration
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  ise-mcp-server:
    build:
      context: ./ise-mcp-server
      dockerfile: Dockerfile
    container_name: ise-mcp-server
    restart: unless-stopped
    
    # Load environment variables from .env file
    env_file:
      - ./ise-mcp-server/.env
    
    # Environment variables (override any from .env file if needed)
    environment:
      - MCP_HOST=0.0.0.0
      
    # Port mapping: host:container
    ports:
      - "8005:8005"
    
    # Security options
    security_opt:
      - no-new-privileges:true
    
    # Resource limits (optional, adjust as needed)
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: '0.5'
        reservations:
          memory: 256M
          cpus: '0.25'
    
    # Logging configuration
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"