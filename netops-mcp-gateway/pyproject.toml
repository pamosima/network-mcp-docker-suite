[project]
name = "netops-mcp-gateway"
version = "0.1.0"
description = "NetOps MCP Gateway — single MCP server that aggregates tools from other suite MCP servers (streamable-http)"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastmcp>=3.2.4",
    "httpx>=0.28.1",
    "uvicorn>=0.35.0",
]
