[project]
name = "prometheus-mcp-server"
version = "0.1.0"
description = "Prometheus MCP server for network metrics queries"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastmcp>=3.4.4",
    "httpx>=0.28.1",
    "uvicorn>=0.48.0",
]
