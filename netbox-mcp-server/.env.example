# NetBox MCP Server Configuration
# Copy this file to .env and update with your actual values

# Required: Your NetBox instance URL
NETBOX_URL=https://netbox.example.com

# Required: Your NetBox API token
NETBOX_TOKEN=your_netbox_token_here

# Optional: Port for SSE server (default: 8001)
MCP_PORT=8001

# Optional: Host for SSE server (default: localhost)  
MCP_HOST=localhost
