[project]
name = "clickhouse-mcp-server"
version = "0.1.0"
description = "ClickHouse MCP server for syslog and log queries"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastmcp>=3.3.1",
    "httpx>=0.28.1",
    "uvicorn>=0.49.0",
]
