# Splunk MCP Server Configuration
# Copy this file to .env and update with your actual values

# ========================================
# REQUIRED CONFIGURATION  
# ========================================

# Splunk server hostname or IP address (REQUIRED)
SPLUNK_HOST=splunk.example.com

# Splunk server port (default: 8089)
SPLUNK_PORT=8089

# Splunk Bearer token for authentication (REQUIRED)
SPLUNK_API_KEY=your_splunk_bearer_token_here

# ========================================
# SPLUNK API CONFIGURATION
# ========================================

# SSL certificate verification (default: false for self-signed certs)
# Set to true in production with valid certificates
SPLUNK_VERIFY_SSL=false

# ========================================
# MCP SERVER CONFIGURATION
# ========================================

# Port for the MCP HTTP server (default: 8006)
MCP_PORT=8006

# Host for the MCP HTTP server (default: 0.0.0.0 for container access)
MCP_HOST=0.0.0.0

# ========================================
# USAGE EXAMPLES
# ========================================
# Once configured, you can use Splunk search and data management tools
# through the MCP protocol for AI-powered log analysis and monitoring

# ========================================
# SECURITY NOTES
# ========================================
# • Never commit .env files to version control
# • Splunk API key provides access to your search and data
# • Use accounts with minimal required permissions
# • Consider using valid SSL certificates in production
# • Monitor API usage and rate limits
# • Rotate credentials regularly for security
