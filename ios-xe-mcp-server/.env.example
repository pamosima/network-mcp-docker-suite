# IOS XE MCP Server Configuration
# ================================
# Copy this file to .env and configure your settings

# ========================================
# Default Credentials (Optional)
# ========================================
# Set default username and password that work across your IOS XE devices
# Host must always be provided per API call for security and flexibility

# Default IOS XE Credentials
IOS_XE_USERNAME=admin
IOS_XE_PASSWORD=your_default_password

# ========================================
# Usage Examples
# ========================================
# With these defaults set, you can use:
#   show_command("show version", "192.168.1.1")
#   config_command(["interface gi0/1", "no shutdown"], "10.1.1.1")
#
# Or override specific credentials:
#   show_command("show version", "192.168.1.1", username="operator")
#   config_command(commands, "10.1.1.1", username="admin", password="special_pwd")

# ========================================
# MCP Server Settings (Optional)
# ========================================
# These settings are for advanced server configuration

# Server Port (default: 8003)
# MCP_SERVER_PORT=8003

# Log Level (default: INFO)
# LOG_LEVEL=INFO

# ========================================
# Security Notes
# ========================================
# • Never commit .env files to version control
# • Use strong passwords and rotate regularly  
# • Consider using SSH keys instead of passwords
# • Limit network access to trusted sources
# • Monitor device access logs regularly
