# Meraki MCP Server - Docker Environment Configuration
# Copy this file to .env and update with your actual values
#
# Usage:
# 1. cp .env.example .env
# 2. Edit .env with your actual values
# 3. docker-compose up -d

# ========================================
# REQUIRED CONFIGURATION
# ========================================

# Your Meraki Dashboard API key (REQUIRED)
# Get this from: https://dashboard.meraki.com/api_docs
MERAKI_KEY=your_actual_meraki_api_key_here

# ========================================
# ACCESS CONTROL
# ========================================

# Role-based access control (noc | sysadmin | all)
# - noc: Limited operational access (monitoring + firmware upgrades)
# - sysadmin: Read-only access (monitoring only)  
# - all: Full API access (firehose mode)
MCP_ROLE=noc

# ========================================
# SERVER CONFIGURATION
# ========================================

# Port for the MCP HTTP server (host side)
# The container always uses port 8000 internally
# Server Port (default: 8000)
MCP_PORT=8000

# Server Host (default: 0.0.0.0)
MCP_HOST=0.0.0.0

# Meraki API Base URL (usually doesn't need to be changed)
MERAKI_BASE_URL=https://api.meraki.com/api/v1
# Host for the MCP HTTP server (usually localhost for local development)
MCP_HOST=localhost
