# Cisco ISE MCP Server Configuration
# Copy this file to .env and update with your actual values

# ========================================
# REQUIRED CONFIGURATION
# ========================================

# ISE Server hostname or IP address (REQUIRED)
ISE_HOST=ise.example.com

# ISE username with API access (REQUIRED)
ISE_USERNAME=your_ise_username

# ISE password (REQUIRED)
ISE_PASSWORD=your_ise_password

# ========================================
# ISE API CONFIGURATION
# ========================================

# ISE API version (default: 1.0)
ISE_VERSION=1.0

# SSL certificate verification (default: False)
# Set to True in production with valid certificates
ISE_VERIFY_SSL=False

# ========================================
# MCP SERVER CONFIGURATION
# ========================================

# Port for the MCP HTTP server (default: 8005)
MCP_PORT=8005

# Host for the MCP HTTP server (default: localhost for local development)
MCP_HOST=localhost

# ========================================
# USAGE EXAMPLES
# ========================================
# Once configured, you can use tools like:
# - ise_get_network_devices() - List network devices
# - ise_get_endpoints() - List managed endpoints  
# - ise_get_active_sessions() - Monitor active sessions
# - ise_search_endpoint_by_mac() - Find device by MAC address
# - ise_get_identity_groups() - List user groups
# - ise_get_authorization_profiles() - List auth profiles

# ========================================
# SECURITY NOTES
# ========================================
# • Never commit .env files to version control
# • ISE credentials provide access to network security data
# • Use accounts with minimal required permissions
# • Enable ERS API on ISE: Administration > Settings > ERS Settings
# • Consider using ISE certificates in production (set ISE_VERIFY_SSL=True)
# • Monitor API usage and rate limits
# • Rotate credentials regularly for security
