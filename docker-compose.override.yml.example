# Optional: copy to docker-compose.override.yml for extra `services:` customizations (volumes, env).
# All MCP services attach to the external `mcp-server` network from docker-compose.yml (no extra fragments).

services: {}
